---
import Navbar from "../../../components/Navbar.astro";
---
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>发展路线 — Linxira OS</title>
  <link rel="icon" type="image/svg+xml" href="/assets/favicon.svg">
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Noto Sans', system-ui, sans-serif; background: #0E1417; color: #e5e7eb; line-height: 1.8; }
    main { max-width: 860px; margin: 0 auto; padding: 100px 2rem 4rem; }
    h1 { font-size: 2.2rem; margin-bottom: 0.5rem; color: #f1f5f9; }
    .subtitle { color: #94a3b8; margin-bottom: 2.5rem; }
    h2 { font-size: 1.4rem; color: #20B8B0; margin: 2.5rem 0 1rem; }
    .card { background: rgba(51,65,85,0.15); border: 1px solid rgba(51,65,85,0.4); border-radius: 12px; padding: 1.25rem 1.5rem; margin-bottom: 1rem; }
    .card h3 { color: #e2e8f0; font-size: 1.05rem; margin-bottom: 0.4rem; }
    .card .tag { display: inline-block; font-size: 0.75rem; padding: 0.15rem 0.6rem; border-radius: 99px; margin-left: 0.5rem; vertical-align: middle; }
    .tag.done { background: rgba(32,184,176,0.2); color: #4DD8D0; }
    .tag.todo { background: rgba(234,179,8,0.15); color: #FACC15; }
    .tag.plan { background: rgba(148,163,184,0.15); color: #94a3b8; }
    .card p { color: #94a3b8; font-size: 0.95rem; }
    .card ul { color: #94a3b8; font-size: 0.95rem; padding-left: 1.2rem; }
    .footer { text-align: center; color: #475569; font-size: 0.85rem; padding: 3rem 0 2rem; }
  </style>
</head>
<body>
  <Navbar lang="zh" active="roadmap" />
  <main>
    <h1>发展路线</h1>
    <p class="subtitle">桌面超算（Desktop Supercomputer）——常规桌面功能完整保留 + 科学计算无头模式，用无损容器复现他人环境，也让别人复现我们。</p>

    <h2>当前状态（2026.08 测试版）</h2>
    <div class="card">
      <h3>测试版已发布 <span class="tag done">已完成</span></h3>
      <ul>
        <li>93 个精选应用（catalog v3），14 大分类</li>
        <li>双内核：linux 7.1.5（主）+ linux-lts 6.18.41（兜底）</li>
        <li>Timeshift + grub-btrfs 快照回滚基础设施</li>
        <li>完整容器生态：Podman / Distrobox / Apptainer / Docker</li>
        <li>自研签名仓库 [linxira] + 17 个自研工具</li>
      </ul>
    </div>

    <h2>正式版（源码自建）</h2>
    <div class="card">
      <h3>核心升级 <span class="tag todo">实施中</span></h3>
      <ul>
        <li>镜像瘦身：4.6G → 2.5-3G（GNOME 改在线安装，Plasma 保持离线可装）</li>
        <li>内存优化：ZRAM 压缩交换 + sysctl 调优（社区验证参数）</li>
        <li>多内核 GRUB 菜单：开机直接选择 linux / linux-lts</li>
        <li>Timeshift 一键配置：linxira-config timeshift enable（每日 3 份）</li>
        <li>容器默认化：Podman + Distrobox + Apptainer 安装时默认勾选</li>
        <li>无头服务器模式：linxira-config headless on/off，桌面零占用给计算</li>
        <li>版本精确定位：OpenJDK 21 LTS、Node.js 预装</li>
      </ul>
    </div>

    <h2>后续迭代</h2>
    <div class="card">
      <h3>多桌面完整选择 <span class="tag plan">规划中</span></h3>
      <p>安装时提供完整的多桌面选择（KDE / GNOME / 更多），桌面环境与平铺窗口管理器分类管理。</p>
    </div>
    <div class="card">
      <h3>恢复模式增强 <span class="tag plan">规划中</span></h3>
      <p>图形化还原界面：一键还原镜像、修复引导、密钥修复；恢复环境默认 KDE 兜底桌面。</p>
    </div>
    <div class="card">
      <h3>性能内核自建 <span class="tag plan">规划中</span></h3>
      <p>基于开源内核（BORE 调度器 + x86-64-v3 优化）自建编译，进我们自己的签名仓库。当前版本使用通用 linux / linux-lts 内核。</p>
    </div>
    <div class="card">
      <h3>平铺窗口管理器 <span class="tag plan">规划中</span></h3>
      <p>Niri / Hyprland 等平铺 WM 进软件列表，装好 KDE 后按需安装，登录界面切换。</p>
    </div>

    <div class="footer">Linxira OS — 面向科研与开发的桌面超算系统</div>
  </main>
</body>
</html>
