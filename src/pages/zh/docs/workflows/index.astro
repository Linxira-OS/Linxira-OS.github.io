---
import Navbar from "../../../../components/Navbar.astro";
---
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>workflows — Linxira OS</title>
  <link rel="icon" type="image/svg+xml" href="/assets/favicon.svg">
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Noto Sans', system-ui, sans-serif; background: #0E1417; color: #e5e7eb; line-height: 1.8; }
    .container { max-width: 720px; margin: 0 auto; padding: 8rem 2rem 4rem; }
    .back { color: #20B8B0; text-decoration: none; font-size: 0.9rem; display: inline-block; margin-bottom: 2rem; }
    h1 { font-size: 2.2rem; font-weight: 700; margin-bottom: 1rem; background: linear-gradient(135deg, #20B8B0, #F36F5D); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
    p { color: #94a3b8; margin-bottom: 1rem; }
    h2 { margin: 2rem 0 0.5rem; font-size: 1.25rem; }
    code { color: #67e8f9; }
    pre { padding: 0.8rem 1rem; margin: 0.75rem 0 1rem; background: #111827; border-left: 3px solid #20B8B0; overflow-x: auto; }
    footer { text-align: center; padding: 3rem 2rem; color: #475569; font-size: 0.85rem; border-top: 1px solid rgba(51,65,85,0.3); }
  </style>
</head>
<body>
  <Navbar lang="zh" active="docs" />

  <div class="container">
    <a href="/zh/docs/" class="back">← 返回文档</a>
    <h1>工作站配置</h1>
    <p>Linxira catalog v2 是 Welcome、Calamares 与 Config Hub 配置选项以及 Shelly 推荐链接的共同来源。可通过 <code>linxira-config workflow help</code> 查看当前版本提供的配置。</p>
    <h2>科学计算</h2>
    <pre><code>linxira-config workflow science</code></pre>
    <p>包括 R、Octave、Gnuplot、ParaView、JupyterLab、NumPy、SciPy、Matplotlib 和 pandas。</p>
    <h2>软件开发</h2>
    <pre><code>linxira-config workflow developer</code></pre>
    <p>包括构建工具、Git、CMake、Code、Rust、Go、Node.js、npm、Python 和 pip。</p>
    <h2>AI 与容器</h2>
    <pre><code>linxira-config workflow ai
linxira-config workflow containers</code></pre>
    <p>AI 配置提供 JupyterLab、PyTorch 和 scikit-learn。厂商 GPU 栈仍需根据硬件明确选择。Podman 与 Distrobox 用于隔离固定版本的项目环境。</p>
    <h2>生物信息学</h2>
    <pre><code>linxira-config workflow bioinformatics</code></pre>
    <p>安装 Apptainer 以运行经过审核、固定版本的工作流容器。Linxira 不会把未审核的 AUR 配方作为官方生信软件包展示。</p>
    <h2>部署边界</h2>
    <p>Linxira 面向个人科学工作站和桌面计算，不推荐用于大规模企业滚动服务器。可复现的服务器式开发应采用固定容器镜像或其他隔离环境。</p>
  </div>

  <footer><p>Linxira OS 直接基于 Arch Linux 构建，是与 Arch Linux 无隶属关系且未经其官方认可的独立项目。</p></footer>
</body>
</html>
