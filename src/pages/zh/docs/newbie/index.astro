---
import PageLayout from "../../../../components/PageLayout.astro";
---
<PageLayout lang="zh" active="docs" title="新手向导 — Linxira OS" maxWidth="760px">
  <a href="/zh/docs/" class="back">← 返回文档</a>
  <h1>新手向导</h1>
  <p>第一次用 Linux？从这里开始——终端基础、查资料、中文环境、桌面选择。</p>

  <h2>Linxira 是什么</h2>
  <p>Linxira 是一台基于 Arch Linux 的桌面超算：默认带 KDE Plasma 桌面，装完即可用。你不需要理解发行版之类的概念，把它当成「一台配好科研软件的 Linux 电脑」就好。</p>

  <h2>终端基础</h2>
  <p>打开终端：KDE 开始菜单 → Terminal（Konsole），或快捷键 <code>Ctrl+Alt+T</code>。</p>
  <p>最常用的命令：</p>
  <table class="keylist">
    <thead>
      <tr><th>命令</th><th>作用</th></tr>
    </thead>
    <tbody>
      <tr><td><code>pwd</code></td><td>显示当前所在目录</td></tr>
      <tr><td><code>ls</code></td><td>列出当前目录的文件</td></tr>
      <tr><td><code>cd 目录名</code></td><td>进入指定目录</td></tr>
      <tr><td><code>sudo 命令</code></td><td>以管理员身份运行（首次会要密码，输入时不显示字符）</td></tr>
      <tr><td><code>sudo pacman -Syu</code></td><td>更新整个系统</td></tr>
      <tr><td><code>sudo pacman -Ss 关键词</code></td><td>搜索软件</td></tr>
      <tr><td><code>sudo pacman -S 包名</code></td><td>安装软件</td></tr>
      <tr><td><code>sudo pacman -Rns 包名</code></td><td>卸载软件</td></tr>
      <tr><td><code>man 命令</code></td><td>查看命令手册（按 q 退出）</td></tr>
    </tbody>
  </table>
  <p>报错怎么办：终端输出是英文——把错误信息复制到搜索框里搜。绝大多数问题都有人遇到过，网上有现成答案。</p>

  <h2>遇到问题去哪查：Arch Wiki</h2>
  <p>Arch 官方维基由全球 Linux 用户共同维护，Linxira 基于 Arch，里面的答案直接适用。打开 <a href="https://wiki.archlinux.org/" target="_blank">https://wiki.archlinux.org/</a>，用页面里的搜索框搜索即可。</p>
  <p>想看中文版：页面左上角语言菜单选「中文（简体）」，对应条目 URL 会带 <code>(简体中文)</code> 后缀。例如 Pacman 条目中文版：<a href="https://wiki.archlinux.org/title/Pacman_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)" target="_blank">https://wiki.archlinux.org/title/Pacman_(简体中文)</a>。</p>
  <p>常用条目速查：</p>
  <table class="keylist">
    <thead>
      <tr><th>主题</th><th>建议搜的条目名</th></tr>
    </thead>
    <tbody>
      <tr><td>装软件</td><td>Pacman</td></tr>
      <tr><td>桌面</td><td>KDE</td></tr>
      <tr><td>输入法</td><td>Fcitx5</td></tr>
      <tr><td>中文环境</td><td>Localization / 简体中文</td></tr>
      <tr><td>显卡</td><td>NVIDIA / AMDGPU</td></tr>
      <tr><td>声音</td><td>PipeWire</td></tr>
      <tr><td>网络</td><td>Network configuration</td></tr>
      <tr><td>引导与恢复</td><td>GRUB / Btrfs</td></tr>
      <tr><td>系统备份</td><td>Timeshift</td></tr>
    </tbody>
  </table>
  <p>官网其他入口：主页导航「下载 / 文档 / 生态 / 路线图 / 博客」；Linxira 源码与仓库在 <a href="https://github.com/Linxira-OS" target="_blank">https://github.com/Linxira-OS</a>。</p>

  <h2>中文环境</h2>
  <p>安装时选了中文 → 语言、区域（<code>zh_CN.UTF-8</code>）与 Fcitx5 输入法已自动配好，无需任何操作。</p>
  <p>装完是英文、想换中文：KDE 系统设置 → 区域与语言（Language）→ 添加「简体中文」→ 拖到列表顶部 → 登出再登录。个别应用仍显示英文时，可补装语言包：</p>
  <pre><code>sudo pacman -S firefox-i18n-zh-cn libreoffice-still-zh-cn</code></pre>
  <p>中文输入法没弄好？按三步排查：</p>
  <p class="term-item"><strong>1) 确认已安装</strong> — 执行 <code>sudo pacman -S fcitx5 fcitx5-chinese-addons fcitx5-configtool</code>。</p>
  <p class="term-item"><strong>2) 启用拼音</strong> — 系统设置 → 输入法（Input Method）→ 添加输入法 → 选「拼音（Pinyin）」。切换输入法快捷键 <code>Ctrl+Space</code>。</p>
  <p class="term-item"><strong>3) 仍不行 → 看诊断报告</strong> — 执行 <code>fcitx5-diagnose | head -n 100</code>，把输出贴给社区或搜索。Linxira 系统统一使用 Fcitx5，不要同时装 ibus 等其他输入法框架，以免冲突。</p>
  <p>自行装了 GNOME 等非 KDE 桌面、输入法不生效时，把下面三行写入 <code>~/.xprofile</code> 后重启会话：</p>
  <pre><code>export GTK_IM_MODULE=fcitx
export QT_IM_MODULE=fcitx
export XMODIFIERS=@im=fcitx</code></pre>
  <p>locale 未生效（<code>locale</code> 命令输出异常）时：<code>sudo nano /etc/locale.gen</code> 取消 <code>zh_CN.UTF-8 UTF-8</code> 行的注释，然后执行 <code>sudo locale-gen</code>。</p>

  <h2>桌面环境选择</h2>
  <p>默认桌面是 KDE Plasma；安装时在「桌面环境」一步二选一（KDE Plasma 或服务器无桌面）；装完后随时可自行安装其他桌面（Plasma、GNOME 等）。「无头模式」只是临时关掉桌面把内存让给计算任务，不是换桌面。详见 <a href="/zh/docs/desktop/">桌面与服务器模式</a>。</p>

  <style>
    h2 { font-size: 1.35rem; color: #20B8B0; margin: 2.5rem 0 0.8rem; }
    h3 { font-size: 1.05rem; color: #cbd5e1; margin: 1.4rem 0 0.5rem; }
    pre { background: #0a0f1a; border: 1px solid rgba(51,65,85,0.5); border-radius: 10px; padding: 1rem 1.2rem; overflow-x: auto; margin: 1rem 0; }
    pre code { background: none; color: #cbd5e1; padding: 0; }
    .note { background: rgba(32,184,176,0.08); border: 1px solid rgba(32,184,176,0.25); border-radius: 10px; padding: 1rem 1.2rem; margin: 1.2rem 0; }
    .note p { color: #cbd5e1; margin: 0; }
    .term-item { margin: 0.9rem 0; color: #94a3b8; line-height: 1.7; }
    .term-item strong { color: #20B8B0; font-weight: 600; }
    .keylist { width: 100%; border-collapse: collapse; margin: 1rem 0 1.5rem; }
    .keylist th, .keylist td { text-align: left; padding: 0.55rem 0.9rem; border-bottom: 1px solid rgba(51,65,85,0.5); vertical-align: top; }
    .keylist th { color: #20B8B0; font-weight: 600; font-size: 0.85rem; white-space: nowrap; }
    .keylist td { color: #cbd5e1; line-height: 1.6; }
    .keylist code { color: #e5e7eb; }
  </style>
</PageLayout>
