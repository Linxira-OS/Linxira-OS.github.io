---
import PageLayout from "../../../../components/PageLayout.astro";
---
<PageLayout lang="zh" active="blog" title="首个实验性正式版：2026.08.14 发布说明 — 博客">
    <a href="/zh/blog/" class="back">← 返回博客</a>
    <div class="meta">2026-08-15 · 公告</div>
    <h1>首个实验性正式版：2026.08.14 发布说明</h1>

    <p>2026 年 8 月 14 日，Linxira OS 发布了<strong>首个实验性正式版</strong>。这是从源码构建到安装使用全链路走通的第一个公开版本，也是 Direct Arch 路线下第一个「可以下载、可以安装、可以日常使用」的 ISO。本文是这份发布的说明：版本信息、发布要点、安装测试结果，以及从测试版以来的演进。它写给两类读者：正在决定要不要下载的人，以及已经装好、想弄清楚自己装的是什么的人。</p>

    <h2>版本信息</h2>
    <p>本次发布的 ISO 文件名为 <code>linxira-2026.08.14-x86_64.iso</code>，大小约 <strong>4.79 GB</strong>，SHA-256 校验值为 <code>b85ffcea99b39402470868a2d63d330613d12ec4068c57b92286a19e403bd145</code>。</p>
    <p>下载渠道有两个：<a href="/zh/download/">官网下载页</a> 与 <a href="https://github.com/Linxira-OS/linxira-iso/releases" target="_blank">linxira-iso GitHub Releases</a>。安装前建议校验 SHA-256 确保文件完整——这一点在完整安装测试中同样被验证过：自研软件安装零报错、签名仓库可用，说明我们交付的软件链路本身是自洽的。</p>
    <p>文件名的命名规则与后续版本一致：<code>linxira-YYYY.MM.DD-x86_64.iso</code>，日期即构建日期。4.79 GB 的体积来自默认装载的 93 个精选应用与双内核——我们选择「预装到位」而不是「装完再折腾」，体积是这份默认软件集的直接代价。校验方式：下载后计算 ISO 的 SHA-256，与上方数值比对一致再制作启动盘。</p>
    <p>完整安装流程（启动盘制作、分区方案、系统要求）见 <a href="/zh/docs/install/">安装指南</a>。推荐分区方案是 btrfs + Timeshift，与系统内置的快照回滚能力配套。按系统要求预留至少 20 GB 存储，推荐 50 GB 以上；启动盘至少需要 8 GB 的 U 盘。</p>

    <h2>发布要点</h2>
    <ul>
      <li><strong>93 个精选应用（catalog v3），14 大分类</strong>：科学计算与游戏软件默认装载，安装即用。</li>
      <li><strong>双内核（linux + linux-lts）+ Timeshift / grub-btrfs 快照回滚</strong>：单一内核更新异常时可回退，GRUB 菜单直接选择快照。</li>
      <li><strong>容器生态默认化</strong>：Podman / Distrobox / Apptainer / Docker 开箱即用，滚动更新的宿主机与固定版本的项目环境互不污染。</li>
      <li><strong>无头计算模式</strong>：<code>linxira-config headless on/off</code> 一键切换，桌面零占用、内存全给计算。</li>
      <li><strong>签名仓库 [linxira] + 17 个自研工具</strong>：自有软件全部通过签名仓库交付，数据库与软件包签名均在 pacman 侧强制校验。</li>
      <li><strong>中文输入法按安装语言自动配置</strong>：安装时选择中文，语言、区域与 Fcitx5 输入法自动配好，无需额外操作。</li>
    </ul>
    <p>这六条共同回答了「为什么这个版本可以称为正式版」：软件生态有了审核目录的支撑，系统安全有了签名仓库与快照回滚的双重保障，计算场景有了无头模式的切换能力，中文用户有了开箱即用的输入环境。</p>
    <p>逐条展开：93 个精选应用不是「装了很多」，而是「装的全是审核过的」——许可证、来源、可用性逐项核对，外部生态（AUR 等）默认关闭；双内核让「更新内核」这件事有了兜底，快照让「更新系统」这件事有了后悔药；容器默认化意味着复现他人环境（以及让别人复现我们）不需要任何额外安装；无头模式把「桌面电脑」与「计算节点」两个身份放进同一台机器；签名仓库保证每个自研软件包都可校验来源；中文输入法随安装语言自动配置，省掉新手最容易卡住的一步。</p>

    <p>把这些要点映射到具体的使用场景会更有实感：生信研究者第一天就能用 Apptainer 容器复现工作流；学生写论文时中文输入开箱即用（Fcitx5 在安装时已配好）；开发者在长假期前更新系统，万一有回归，重启进 GRUB 选更新前的 Timeshift 快照即可；跑通宵训练任务的研究者开启无头模式，把每 MB 内存都让给进程，第二天早上再切回桌面。这些不是宣传话术——它们正是安装测试与底层架构被构建出来要支撑的路径。</p>

    <h2>安装测试结果</h2>
    <p>发布前我们执行了完整的安装测试，结论是：<strong>自研软件安装零报错、签名仓库可用</strong>。测试覆盖了从 Live 环境启动、Calamares 安装、首次进入系统到自研软件安装与更新的完整链路，没有遇到阻断性错误。</p>
    <p>「实验性」的含义在这里需要说清楚：这个版本证明了链路是通的，但它仍是第一个公开的直基 Arch 正式版，覆盖的硬件与使用场景有限。我们把它放出来，是希望更多人在真实环境里使用、反馈，再据此迭代后续版本。</p>
    <p>测试的具体路径与普通用户完全一致：用 Rufus / balenaEtcher 或 <code>dd</code> 制作启动盘，从 USB 启动进入 Live 环境，在 Welcome 或应用菜单中打开「安装 Linxira OS」，选择语言与键盘布局、分区方案（推荐 btrfs + Timeshift）、从 catalog v2 选择可选的审核配置并创建用户账户，等待安装完成，重启。每一个环节都实际执行过，而不是只验证了「安装器能打开」。</p>

    <p>测试里有两个细节值得单独说明，因为它们是「能不能启动」这种快速检查看不到的部分。其一，自研软件安装走的是签名仓库 [linxira]——包下载、pacman 校验签名、安装、启动，全程零报错；这就是「签名仓库可用」的实际含义。其二，更新同样走 pacman 链路验证过，确认更新链路从仓库到已安装包全程可追踪。两者合起来覆盖了每个用户会反复执行的两个操作：安装与更新。</p>

    <h2>从测试版以来的演进</h2>
    <p>8.14 之前的两个测试版已经为今天打下了基线：</p>
    <ul>
      <li><strong>8.12（测试版基线）</strong>：KDE Plasma 桌面 + 预装软件离线可用；中国镜像自动 fallback；[linxira] 数据库在安装期预同步。测试版从这里开始具备「装完就能用」的形态。</li>
      <li><strong>8.13（测试版）</strong>：8.12 构建的增量修复——启动 / 关机标识与终端字符画定稿；自研软件全部可正常安装与启动验证；签名仓库更新链路（pacman）可追踪。品牌与软件安装的可信度在这个版本收敛。</li>
      <li><strong>8.14（实验性正式版）</strong>：在前两者之上完成完整安装测试，正式对外发布。</li>
    </ul>
    <p>更早的 8.09 RC 候选版（17 次迭代）则是这一切的前身：安装器、品牌、默认应用集在那 17 轮迭代里逐轮收敛。完整的发布记录见 <a href="/zh/releases/">发布日志</a>。</p>
    <p>从测试版到正式版的变化，本质是三个「可信」的建立：8.12 建立了「装完能用」的可信（桌面与预装软件离线可用）；8.13 建立了「装的东西可信」的可信（自研软件全部可安装可启动、更新链路可追踪）；8.14 在此基础上完成了「整条链路可信」的验证（完整安装测试通过）。每一步都踩在前一步的结论上，没有跳过任何环节。</p>

    <h2>已知边界与后续</h2>
    <p>当前版本的边界是明确的：它仍是<strong>实验性正式版</strong>，不是稳定版承诺。后续版本将按 <a href="/zh/docs/building/">构建指南</a> 由用户从源码自建，项目提供构建脚本与产物复现手段，而不是无休止地滚动发布新 ISO。</p>
    <p>换句话说：这次发布的版本号是「第一个」，但它不会像传统发行版那样有一个固定的支持周期。我们的更新模型是滚动更新（Arch 直连上游）+ 快照回滚（Timeshift / grub-btrfs）+ 可复现构建（构建指南），三者配合才是这套系统的完整形态。</p>
    <p>具体到用户侧意味着几点：系统持续接收 Arch 上游更新，「8.14 安装完成」的状态是起点而不是冻结的版本；每次重要更新前 linxira-update 默认创建 Timeshift 快照，坏更新是一次重启就能撤销的事，而不是重装系统；如果哪天想要这个版本的确切状态，构建指南可以从源码复现出同样的 ISO。所以这份发布说明描述的是一个基线，不是天花板。</p>
    <p>如果你已经安装或即将安装 8.14：装完后第一步建议先阅读 <a href="/zh/docs/quick-start/">快速开始</a>，启用签名仓库、更新系统、按需安装软件。遇到问题先看 <a href="/zh/docs/faq/">常见问题</a>，再不行就去社区反馈。</p>
    <p>反馈的内容对我们特别有价值的是这几种：你用的硬件型号（尤其是显卡与无线网卡）能否正常工作、安装过程中卡住的步骤、以及「装完第一周」的使用体验。实验性正式版的后续版本会按构建指南持续迭代，你的反馈会直接进入下一轮构建的优先级。每一条「这台设备能用 / 这台设备有问题」的报告，都在帮我们缩小「链路通了」与「链路处处通」之间的差距。</p>
<style>
    .meta { color: #64748b; font-size: 0.85rem; margin-bottom: 2rem; }
    h2 { font-size: 1.4rem; color: #e2e8f0; margin: 2rem 0 0.8rem; padding-top: 1rem; border-top: 1px solid rgba(51,65,85,0.3); }
    h3 { font-size: 1.15rem; color: #e2e8f0; margin: 1.5rem 0 0.5rem; }
    strong { color: #e2e8f0; }
    li { margin-bottom: 0.3rem; }
    table { width: 100%; border-collapse: collapse; margin: 1rem 0; }
    th { text-align: left; padding: 0.6rem; border-bottom: 2px solid rgba(51,65,85,0.5); color: #e2e8f0; font-size: 0.9rem; }
    td { padding: 0.6rem; border-bottom: 1px solid rgba(51,65,85,0.3); color: #94a3b8; font-size: 0.9rem; }
    pre { background: #0a0f1a; border: 1px solid rgba(51,65,85,0.5); border-radius: 10px; padding: 1rem 1.2rem; overflow-x: auto; margin: 1rem 0; }
    pre code { background: none; color: #cbd5e1; padding: 0; }
  </style>
</PageLayout>
